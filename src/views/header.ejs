<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles.css">
    <link rel="icon" href="/images/icon.png" type="x-icon">
    <title><%= title %> - TaskManager </title>
</head>
<body style="min-height: 100vh;">
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container">
            <% if (usuario) { %>
                <a class="navbar-brand fw-bold" href="/profile">TaskManager</a>
            <% } else { %>
                <a class="navbar-brand fw-bold" href="/login">TaskManager</a>
            <% } %>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <% if (usuario) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/profile">Perfil de usuario</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/api/logout">Cerrar sesión</a>
                        </li>
                    
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" href="/login">Iniciar sesión</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/register">Registrate</a>
                        </li>
                    <% } %>
                     
                </ul>
            </div>
        </div>
    </nav>

    <main class="container content d-flex align-items-center justify-content-center">

    